version: "3.8"
services:
  waymark:
    build:
      context: .
      args:
        GIT_HASH: ${GIT_HASH:-}
        GIT_REPO: ${GIT_REPO:-}
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
